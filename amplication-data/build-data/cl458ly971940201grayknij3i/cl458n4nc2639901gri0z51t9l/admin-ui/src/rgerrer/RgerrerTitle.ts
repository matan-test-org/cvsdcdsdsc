import { Rgerrer as TRgerrer } from "../api/rgerrer/Rgerrer";

export const RGERRER_TITLE_FIELD = "id";

export const RgerrerTitle = (record: TRgerrer): string => {
  return record.id || record.id;
};
